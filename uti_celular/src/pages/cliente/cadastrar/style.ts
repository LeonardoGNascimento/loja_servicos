import styled from "styled-components";

export const CadastroClienteContainer = styled.div`
  padding: 3rem;
  margin: 3rem;
  box-shadow: ${({ theme }) => theme.shadow};
  border-radius: ${({ theme }) => theme.borderRadius};
`;
